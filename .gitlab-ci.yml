# Pipelines for this repo focus on keeping vendored upstream artifacts in
# sync with their pinned versions. New jobs should follow the same
# branch-based dispatch pattern used by `statusline-sync`.

stages:
  - sync
  - test

statusline-sync:
  stage: sync
  image: alpine:3.20
  timeout: 10m
  before_script:
    - apk add --no-cache bash curl git
  script:
    # alpine's default shell is ash; bashisms (e.g. `[[ =~ ]]`) need explicit
    # bash. Quoted heredoc delimiter prevents the outer shell from expanding
    # variables — bash receives them literally and resolves them itself.
    - |
      bash <<'JOBSCRIPT'
      set -euo pipefail
      if [[ "${CI_COMMIT_REF_NAME}" =~ ^renovate/statusline- ]]; then
        echo "==> statusline sync branch detected: ${CI_COMMIT_REF_NAME}"
        bash assets/sync-statusline.sh
        if git diff --quiet -- dot_claude/executable_statusline.sh \
             private_Documents/development/container-dotfiles/dotfiles/dot_claude/executable_statusline.sh; then
          echo "==> vendored body already matches the pinned version; nothing to push"
          exit 0
        fi
        if [ -z "${VENDOREDFILE_SYNC_TOKEN:-}" ]; then
          echo "ERROR: VENDOREDFILE_SYNC_TOKEN CI variable is not set;" >&2
          echo "       see docs/automation/renovate-gitlab-runner-setup.md" >&2
          exit 1
        fi
        export GIT_AUTHOR_NAME="Renovate Bot"
        export GIT_AUTHOR_EMAIL="service_account_group_65498163_3cf840e6e3ac6e34bec3beb607caff8a@noreply.gitlab.com"
        export GIT_COMMITTER_NAME="Renovate Bot"
        export GIT_COMMITTER_EMAIL="service_account_group_65498163_3cf840e6e3ac6e34bec3beb607caff8a@noreply.gitlab.com"
        git add \
          dot_claude/executable_statusline.sh \
          private_Documents/development/container-dotfiles/dotfiles/dot_claude/executable_statusline.sh
        git commit --amend --no-edit
        git remote set-url origin "https://oauth2:${VENDOREDFILE_SYNC_TOKEN}@gitlab.com/${CI_PROJECT_PATH}.git"
        git push \
          --force-with-lease="refs/heads/${CI_COMMIT_REF_NAME}:${CI_COMMIT_SHA}" \
          origin "HEAD:${CI_COMMIT_REF_NAME}"
        echo "==> amended commit pushed; superseding pipeline will validate"
      else
        echo "==> non-statusline branch; validating vendored body against pinned version"
        bash assets/sync-statusline.sh --check
      fi
      JOBSCRIPT
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "push"'

beads-kanban-pin-sync:
  stage: sync
  image: alpine:3.20
  timeout: 10m
  before_script:
    - apk add --no-cache bash curl git
  script:
    # Renovate bumps only the version sentinel in the three install sites; it
    # cannot compute the release checksum. Refresh EXPECTED_SHA here, before
    # platformAutomerge can fire.
    - |
      bash <<'JOBSCRIPT'
      set -euo pipefail
      BK_FILES=(
        .chezmoiscripts/run_onchange_after_install_better_beads_kanban.sh.tmpl
        .chezmoiscripts/run_onchange_after_install_better_beads_kanban.ps1.tmpl
        private_Documents/development/container-dotfiles/devcontainers/gitlab.com/servers-homelab/homelab-IaC/dot_devcontainer/devcontainer-common.sh
      )
      if [[ "${CI_COMMIT_REF_NAME}" =~ ^renovate/beads-kanban- ]]; then
        echo "==> beads kanban pin branch detected: ${CI_COMMIT_REF_NAME}"
        bash assets/sync-beads-kanban-pin.sh --write
        if git diff --quiet -- "${BK_FILES[@]}"; then
          echo "==> pinned checksum already matches the release; nothing to push"
          exit 0
        fi
        if [ -z "${VENDOREDFILE_SYNC_TOKEN:-}" ]; then
          echo "ERROR: VENDOREDFILE_SYNC_TOKEN CI variable is not set;" >&2
          echo "       see docs/automation/renovate-gitlab-runner-setup.md" >&2
          exit 1
        fi
        export GIT_AUTHOR_NAME="Renovate Bot"
        export GIT_AUTHOR_EMAIL="service_account_group_65498163_3cf840e6e3ac6e34bec3beb607caff8a@noreply.gitlab.com"
        export GIT_COMMITTER_NAME="Renovate Bot"
        export GIT_COMMITTER_EMAIL="service_account_group_65498163_3cf840e6e3ac6e34bec3beb607caff8a@noreply.gitlab.com"
        git add "${BK_FILES[@]}"
        git commit --amend --no-edit
        git remote set-url origin "https://oauth2:${VENDOREDFILE_SYNC_TOKEN}@gitlab.com/${CI_PROJECT_PATH}.git"
        git push \
          --force-with-lease="refs/heads/${CI_COMMIT_REF_NAME}:${CI_COMMIT_SHA}" \
          origin "HEAD:${CI_COMMIT_REF_NAME}"
        echo "==> amended commit pushed; superseding pipeline will validate"
      else
        echo "==> non-beads-kanban branch; validating pinned checksum against the release"
        bash assets/sync-beads-kanban-pin.sh --check
      fi
      JOBSCRIPT
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "push"'

browser-policy-sync:
  stage: sync
  image: alpine:3.20
  timeout: 10m
  before_script:
    - apk add --no-cache git python3
  script:
    - |
      set -eu
      case "${CI_COMMIT_REF_NAME}" in
        renovate/browser-policies-*)
          echo "==> browser policy sync branch detected: ${CI_COMMIT_REF_NAME}"
          python3 assets/sync-browser-policies.py --write
          if git diff --quiet -- configs/browser-policies/justthebrowser; then
            echo "==> vendored browser policies already match the pinned version; nothing to push"
            exit 0
          fi
          if [ -z "${VENDOREDFILE_SYNC_TOKEN:-}" ]; then
            echo "ERROR: VENDOREDFILE_SYNC_TOKEN CI variable is not set;" >&2
            echo "       see docs/automation/renovate-gitlab-runner-setup.md" >&2
            exit 1
          fi
          export GIT_AUTHOR_NAME="Renovate Bot"
          export GIT_AUTHOR_EMAIL="service_account_group_65498163_3cf840e6e3ac6e34bec3beb607caff8a@noreply.gitlab.com"
          export GIT_COMMITTER_NAME="Renovate Bot"
          export GIT_COMMITTER_EMAIL="service_account_group_65498163_3cf840e6e3ac6e34bec3beb607caff8a@noreply.gitlab.com"
          git add configs/browser-policies/justthebrowser
          git commit --amend --no-edit
          git remote set-url origin "https://oauth2:${VENDOREDFILE_SYNC_TOKEN}@gitlab.com/${CI_PROJECT_PATH}.git"
          git push \
            --force-with-lease="refs/heads/${CI_COMMIT_REF_NAME}:${CI_COMMIT_SHA}" \
            origin "HEAD:${CI_COMMIT_REF_NAME}"
          echo "==> amended commit pushed; superseding pipeline will validate"
          ;;
        *)
          echo "==> non-browser-policy branch; validating vendored policies against pinned version"
          python3 assets/sync-browser-policies.py --check
          ;;
      esac
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "push"'

beads-release-notes:
  stage: sync
  image: alpine:3.20
  timeout: 5m
  before_script:
    - apk add --no-cache ca-certificates python3
  script:
    - python3 assets/check-beads-release-notes.py
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME =~ /^renovate\/beads-core-/'
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /^renovate\/beads-core-/'
    - when: never

opencode-v1-policy:
  stage: test
  image: python:3.13.7-bookworm
  needs: []
  interruptible: true
  timeout: 5m
  script:
    - python3 -m unittest tests.test_opencode_v1.OpenCodePolicyTests
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "push"'

linux-fast:
  stage: test
  image: python:3.13.7-bookworm
  needs: []
  interruptible: true
  timeout: 10m
  before_script:
    - |
      set -eu
      apt-get update -qq
      apt-get install -y --no-install-recommends jq
      jq --version
      test "$(uname -m)" = x86_64
      curl --fail --silent --show-error --location --retry 2 --connect-timeout 10 --max-time 60 https://nodejs.org/dist/v24.21.0/node-v24.21.0-linux-x64.tar.xz -o /tmp/node.tar.xz
      printf '%s\n' 'fd8e59d5a511510f6a298afb548f18c7d2b1be404d8b4a27d94fbe49f56cb2d6  /tmp/node.tar.xz' | sha256sum --check --strict
      tar -xJf /tmp/node.tar.xz --strip-components=2 -C /usr/local/bin node-v24.21.0-linux-x64/bin/node
      node --version
  script:
    - mkdir -p ci-artifacts
    - ./assets/run-tests.sh fast --require-capabilities --report-file ci-artifacts/linux-fast.json
  artifacts:
    when: always
    expire_in: 7 days
    paths:
      - ci-artifacts/linux-fast.json
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME =~ /^renovate\//'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_LABELS =~ /(^|,)renovate(,|$)/i'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /^renovate\//'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH && $CI_COMMIT_BRANCH != $CI_DEFAULT_BRANCH && $CI_OPEN_MERGE_REQUESTS'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH && $CI_COMMIT_BRANCH != $CI_DEFAULT_BRANCH'

linux-all:
  stage: test
  image: python:3.13.7-bookworm
  needs: []
  interruptible: true
  allow_failure: true
  timeout: 30m
  script:
    - mkdir -p ci-artifacts
    - ./assets/run-tests.sh all --require-capability bash --require-capability git --require-capability sh --report-file ci-artifacts/linux-all.json
  artifacts:
    when: always
    expire_in: 7 days
    paths:
      - ci-artifacts/linux-all.json
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME =~ /^renovate\//'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_LABELS =~ /(^|,)renovate(,|$)/i'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /^renovate\//'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual

devcontainer-smoke:
  stage: test
  tags:
    - devcontainer-smoke
  needs: []
  interruptible: true
  allow_failure: true
  timeout: 30m
  variables:
    DEVCONTAINER_SMOKE: "1"
    DEVCONTAINER_SMOKE_ARTIFACTS: "ci-artifacts/devcontainer-smoke"
  script:
    - mkdir -p ci-artifacts/devcontainer-smoke
    - python3 assets/devcontainer-smoke.py --run
  artifacts:
    when: always
    expire_in: 7 days
    paths:
      - ci-artifacts/devcontainer-smoke/
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME =~ /^renovate\//'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_LABELS =~ /(^|,)renovate(,|$)/i'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /^renovate\//'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual

windows-all:
  stage: test
  tags:
    - saas-windows-medium-amd64
  needs: []
  interruptible: true
  allow_failure: true
  timeout: 30m
  script:
    - New-Item -ItemType Directory -Force ci-artifacts | Out-Null
    - pwsh -NoProfile -File ./assets/run-tests.ps1 all --require-capability git --require-capability pwsh --report-file ci-artifacts/windows-all.json
  artifacts:
    when: always
    expire_in: 7 days
    paths:
      - ci-artifacts/windows-all.json
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME =~ /^renovate\//'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_LABELS =~ /(^|,)renovate(,|$)/i'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH =~ /^renovate\//'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: manual
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
