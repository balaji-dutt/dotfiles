# Managed by chezmoi. The shared pin lives in .chezmoidata.yaml. macOS only:
# WSL2 gets the equivalent fragment from ansible/wsl-playbook.yml.
[tools]
"github:DeusData/codebase-memory-mcp" = "{{ .codebase_memory_mcp_version }}"
