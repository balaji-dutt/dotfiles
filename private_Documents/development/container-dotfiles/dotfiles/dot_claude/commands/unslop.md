---
description: Activate unslop humanized writing mode
---

Use the `unslop` skill.

Activate balanced unslop mode for visible prose in this conversation. Strip AI-isms, preserve technical substance exactly, and keep code, commands, URLs, file paths, headings, numbers, and error messages unchanged.

If the invocation includes text, rewrite that text now. Otherwise, apply the mode to future assistant responses until the user says `stop unslop`, `normal mode`, or invokes a different unslop mode.
