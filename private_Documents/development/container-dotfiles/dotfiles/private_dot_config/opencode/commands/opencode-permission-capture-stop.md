---
description: Stop recording OpenCode permission prompts
---

Use the `permission_capture_stop` tool with `confirm` set to `stop`.

Report the capture status and file path. Do not clear captured records.
