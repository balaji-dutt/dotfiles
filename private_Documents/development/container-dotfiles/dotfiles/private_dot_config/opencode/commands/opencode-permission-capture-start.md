---
description: Start recording OpenCode permission prompts
---

Use the `permission_capture_start` tool with `confirm` set to `start` and
`scope` set to `bash` unless the user explicitly requests all permission types.

Report the capture status and file path. Do not edit permission config yet.
