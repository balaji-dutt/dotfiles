---
# =============================================================================
# Ubuntu-specific Utilities
# =============================================================================

- name: Install Ubuntu extra apt packages
  become: true
  ansible.builtin.apt:
    name: "{{ ubuntu_apt_packages }}"
    state: present

